# Contributing

## Fix and Formatting

See also:

* [ci.yml](.github/workflows/ci.yml)
* [fix.sh](fix.sh)

### Formatting

I'm using the nightly version of `rustfmt`:

```terminaloutput
$ cargo +nightly fmt
```

and/or `./format.sh`

### Clippy

I'm using the stable version of `clippy`

```terminaloutput
$ cargo +nightly fmt
```
